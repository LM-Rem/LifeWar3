# Frozen minimap index baseline

Unmodified `public/minimap-cache.js` before temporary-array removal and stale-index release on 2026-09-25. See `manifest.json` for source commit and SHA256. Use with `minimap-profile.mjs --baseline tests/baselines/minimap-index/minimap-cache.js`; the renderer and packet semantics are otherwise the same.
