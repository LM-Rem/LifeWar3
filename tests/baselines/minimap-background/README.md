# Background invalidation baseline

Unmodified `public/minimap-cache.js` before background style changes began reusing contributor indexes and dirtying affected tiles. Commit and SHA256 are recorded in `manifest.json`. The background benchmark routes this file while keeping the current renderer and protocol.
